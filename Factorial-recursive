function factorial(num) {
  if(Math.sign(num) === -1) {
    return 'Factorial cannot be defined for negative number.'
  }
  if (num === 0 || num === 1) {
    return 1;
  } else {
    return num * factorial(num - 1)
  }
}

console.log(factorial(4))